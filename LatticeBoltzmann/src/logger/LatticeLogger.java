package logger;

import lattice.Lattice;

/** log information about a given lattice */
public class LatticeLogger {

	/**
	 * 
	 * @param l
	 *            the lattice to log
	 * @param obj
	 *            other things to look at, change as need be. (ie: flow meters)
	 */
	public static void log(Lattice l, Object... obj) {
	}
}
